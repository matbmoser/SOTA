version: "3.9"

services:
  mysql:
    image: mysql
    container_name: sota-mysql-bbdd
    environment:
      MYSQL_DATABASE: "sotadb"
      # So you don't have to use root, but you can if you like
      MYSQL_USER: "dbadmin"
      # You can use whatever password you like
      MYSQL_PASSWORD: "GT9zEN(FdxSAmWiy"
      # Password for root access
      MYSQL_ROOT_PASSWORD: "s0t42022"
    ports:
      # <Port exposed> : < MySQL Port running inside container>
      - "3306:3306"
      # Where our data will be persisted
    volumes:
      - ./sota-webapp/data:/var/lib/mysql

  php-my-admin:
    image: phpmyadmin
    container_name: sota-phpmyadmin
    ports:
      - 7777:80
    environment:
      - PMA_HOST=mysql
    depends_on:
      - mysql

  # The existing database container configuration...
  php-apache:
    container_name: sota-php-webapp
    build:
      context: ./sota-webapp/php
    ports:
      - "8080:80"
      - "3001:8001"
      - "3000:8000"
      - "3002:8002"
    volumes:
      - ./sota-webapp/src:/var/www/sota-webapp
      - ./sota-webapp/apache/default.conf:/etc/apache2/sites-enabled/000-default.conf
    depends_on:
      - mysql
    networks:
      - default
      - appNet

  digital-twin:
    container_name: sota-digital-twin
    build:
      context: ./sota-digital-twin/python
    ports:
      - "3333:80"
    volumes:
      - ./sota-digital-twin/src:/var/www/sota-digital-twin
      - ./sota-digital-twin/deviceManager:/usr/src/app
      - ./sota-digital-twin/apache/default.conf:/etc/apache2/sites-enabled/000-default.conf
    depends_on:
      - mysql
    networks:
      - default
      - appNet

networks:
  default: # this network (app2)
    driver: bridge
  appNet: # external network (app1)
    external: true

# Names our volume
volumes:
  data:
